import {
  to = aws_dynamodb_table.dynamodb-terraform-state-lock
  id = local.dynamodb_table_name
}

import {
  to = module.tf_state.aws_s3_bucket.this[0]
  id = local.bucket_name
}

import {
  to = module.tf_state.aws_s3_bucket_lifecycle_configuration.this[0]
  id = local.bucket_name
}

import {
  to = module.tf_state.aws_s3_bucket_policy.this[0]
  id = local.bucket_name
}

import {
  to = module.tf_state.aws_s3_bucket_public_access_block.this[0]
  id = local.bucket_name
}

import {
  to = module.tf_state.aws_s3_bucket_server_side_encryption_configuration.this[0]
  id = local.bucket_name
}

import {
  to = module.tf_state.aws_s3_bucket_versioning.this[0]
  id = local.bucket_name
}
